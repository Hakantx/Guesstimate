# Benchmarks

Generated by `make bench`. Every figure here comes from the run described below; none of it is copied forward from an earlier one.

| Provenance | |
|---|---|
| Commit | `36ff6e4768ed` (tree dirty) |
| Ruleset | length 5, alphabet `1234`, repeats on (1024 codes) |
| Sample | 300 secrets |
| Seed | 20260805 |
| Machine | macOS-13.7.8-x86_64-i386-64bit |
| Processor | i386, 4 cores |
| Python | CPython 3.12.13 |
| Run started | 2026-08-05T20:31:19+00:00 |

## Guess counts

| Solver | Mean | Median | Sample max | SD | vs floor |
|---|---|---|---|---|---|
| `entropy/restricted` | 3.893 | 4.0 | 5 | 0.666 | +1.580 |
| `expected-size/restricted` | 3.930 | 4.0 | 5 | 0.638 | +1.616 |
| `minimax/restricted` | 4.030 | 4.0 | 6 | 0.696 | +1.716 |
| `random/restricted` | 4.151 | 4.0 | 7 | 0.788 | +1.838 |

Information floor for this ruleset: **2.314** guesses (log2(1024) / log2(outcomes)). No strategy can average less; the `vs floor` column is how far each one is from a bound that assumes every guess splits the space perfectly evenly.

**`Sample max` is a lower bound on the true worst case**, not the worst case. It is the hardest of the 300 secrets drawn, out of 1024; the real maximum can only be higher. This matters most for minimax, whose entire justification is the worst case -- the one column a sample cannot produce. Run `make bench ARGS="--full"` for a figure that earns the name.

## Paired comparison

| Solver vs `random/restricted` | Mean diff | SD | 95% CI | W/L/T | Significant |
|---|---|---|---|---|---|
| `minimax/restricted` | +0.121 | 0.770 | [+0.034, +0.208] | 153/111/36 | yes |
| `expected-size/restricted` | +0.221 | 0.681 | [+0.144, +0.298] | 166/96/38 | yes |
| `entropy/restricted` | +0.258 | 0.748 | [+0.173, +0.343] | 173/87/40 | yes |

Positive means fewer guesses than the baseline. Every solver played the same secrets in the same order, so these are per-secret differences rather than differences of means -- which removes the variation from some secrets simply being harder, and is what makes a gap of a few hundredths of a guess measurable at all.

## Timing

| Solver | Cold open | Warm game (mean) | Warm game (median) | Per turn |
|---|---|---|---|---|
| `entropy/restricted` | 7.29s | 96ms | 70ms | 23ms |
| `expected-size/restricted` | 6.93s | 90ms | 66ms | 21ms |
| `minimax/restricted` | 7.39s | 89ms | 83ms | 20ms |
| `random/restricted` | 0ms | 7ms | 7ms | 0ms |

Cold open is the first guess of the run, searching the whole space with an empty opening cache. Every warm game reuses that opening, so the two are reported apart -- averaging them would fold a single outlier worth tens of seconds into a per-game figure.

## Candidate collapse

| Solver | After 1 | After 2 | After 3 | After 4 | After 5 |
|---|---|---|---|---|---|
| `entropy/restricted` | 97.1 | 8.3 | 1.3 | 1.0 | 1.0 |
| `expected-size/restricted` | 95.2 | 8.1 | 1.3 | 1.0 | 1.0 |
| `minimax/restricted` | 98.9 | 8.7 | 1.5 | 1.0 | 1.0 |
| `random/restricted` | 102.8 | 11.1 | 2.0 | 1.1 | 1.0 |

Mean candidates still possible after each guess. The first guess does most of the work; what separates the strategies is how evenly they split what is left.
