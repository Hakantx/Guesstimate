# Benchmarks

Generated by `make bench`. Every figure here comes from the run described below; none of it is copied forward from an earlier one.

| Provenance | |
|---|---|
| Commit | `ef75317f1648` (tree dirty) |
| Ruleset | length 4, alphabet `123456789`, repeats off (3024 codes) |
| Sample | 3024 secrets |
| Seed | 20260805 |
| Machine | macOS-13.7.8-x86_64-i386-64bit |
| Processor | i386, 4 cores |
| Python | CPython 3.12.13 |
| Run started | 2026-08-09T20:26:14+00:00 |

## Guess counts

| Solver | Mean | Median | Worst | SD | vs entropy floor | vs counting floor |
|---|---|---|---|---|---|---|
| `entropy/restricted/matrix` | 5.008 | 5.0 | 7 | 0.818 | +1.971 | +0.860 |
| `expected-size/restricted/matrix` | 5.011 | 5.0 | 7 | 0.820 | +1.974 | +0.863 |
| `minimax/restricted/matrix` | 5.044 | 5.0 | 7 | 0.830 | +2.007 | +0.896 |
| `random/restricted/matrix` | 5.161 | 5.0 | 9 | 0.928 | +2.124 | +1.013 |

Two lower bounds, both sound, one much tighter.

**Entropy floor 3.037** -- log2(3024) / log2(outcomes), assuming every guess splits the space perfectly evenly. True but weak: no single code splits the candidates into equal parts, let alone repeatedly.

**Counting floor 4.147** -- a strategy is a decision tree whose nodes have at most one winning edge, so at most `(outcomes - 1) ** (j - 1)` secrets can be solved on guess `j`. Packing the space as shallowly as those caps allow bounds the mean. It uses nothing about any position, which is what makes it sound.

## Paired comparison

| Solver vs `random/restricted/matrix` | Mean diff | SD | 95% CI | W/L/T | Significant |
|---|---|---|---|---|---|
| `minimax/restricted/matrix` | +0.117 | 0.937 | [+0.083, +0.150] | 1516/1234/274 | yes |
| `expected-size/restricted/matrix` | +0.150 | 0.926 | [+0.117, +0.183] | 1563/1186/275 | yes |
| `entropy/restricted/matrix` | +0.153 | 0.927 | [+0.120, +0.186] | 1574/1180/270 | yes |

Positive means fewer guesses than the baseline. Every solver played the same secrets in the same order, so these are per-secret differences rather than differences of means -- which removes the variation from some secrets simply being harder, and is what makes a gap of a few hundredths of a guess measurable at all.

## Timing

| Solver | Cold open | Warm game (mean) | Warm game (median) | Per turn |
|---|---|---|---|---|
| `entropy/restricted/matrix` | 0ms | 15ms | 14ms | 3ms |
| `expected-size/restricted/matrix` | 0ms | 11ms | 13ms | 2ms |
| `minimax/restricted/matrix` | 0ms | 11ms | 12ms | 2ms |
| `random/restricted/matrix` | 0ms | 0ms | 0ms | 0ms |

Cold open is the first guess of the run, searching the whole space with an empty opening cache. Every warm game reuses that opening, so the two are reported apart -- averaging them would fold a single outlier worth tens of seconds into a per-game figure.

## Candidate collapse

| Solver | After 1 | After 2 | After 3 | After 4 | After 5 |
|---|---|---|---|---|---|
| `entropy/restricted/matrix` | 537.6 | 88.6 | 12.2 | 1.9 | 1.0 |
| `expected-size/restricted/matrix` | 537.6 | 88.6 | 12.2 | 1.9 | 1.0 |
| `minimax/restricted/matrix` | 537.6 | 88.8 | 12.5 | 2.0 | 1.0 |
| `random/restricted/matrix` | 537.2 | 88.7 | 13.9 | 2.6 | 1.1 |

Mean candidates still possible after each guess. The first guess does most of the work; what separates the strategies is how evenly they split what is left.
