# Benchmarks

Generated by `make bench`. Every figure here comes from the run described below; none of it is copied forward from an earlier one.

| Provenance | |
|---|---|
| Commit | `ef75317f1648` (tree dirty) |
| Ruleset | length 4, alphabet `0123456789`, repeats off (5040 codes) |
| Sample | 5040 secrets |
| Seed | 20260805 |
| Machine | macOS-13.7.8-x86_64-i386-64bit |
| Processor | i386, 4 cores |
| Python | CPython 3.12.13 |
| Run started | 2026-08-09T21:30:14+00:00 |

## Guess counts

| Solver | Mean | Median | Worst | SD | vs floor |
|---|---|---|---|---|---|
| `entropy/restricted/matrix` | 5.314 | 5.0 | 8 | 0.869 | +2.083 |
| `expected-size/restricted/matrix` | 5.319 | 5.0 | 8 | 0.870 | +2.089 |
| `minimax/restricted/matrix` | 5.354 | 5.0 | 8 | 0.886 | +2.123 |
| `random/restricted/matrix` | 5.458 | 5.0 | 9 | 0.980 | +2.228 |

Information floor for this ruleset: **3.230** guesses (log2(5040) / log2(outcomes)). No strategy can average less; the `vs floor` column is how far each one is from a bound that assumes every guess splits the space perfectly evenly.

## Paired comparison

| Solver vs `random/restricted/matrix` | Mean diff | SD | 95% CI | W/L/T | Significant |
|---|---|---|---|---|---|
| `minimax/restricted/matrix` | +0.105 | 0.988 | [+0.077, +0.132] | 2457/2193/390 | yes |
| `expected-size/restricted/matrix` | +0.139 | 0.967 | [+0.113, +0.166] | 2512/2115/413 | yes |
| `entropy/restricted/matrix` | +0.145 | 0.967 | [+0.118, +0.171] | 2528/2099/413 | yes |

Positive means fewer guesses than the baseline. Every solver played the same secrets in the same order, so these are per-secret differences rather than differences of means -- which removes the variation from some secrets simply being harder, and is what makes a gap of a few hundredths of a guess measurable at all.

## Timing

| Solver | Cold open | Warm game (mean) | Warm game (median) | Per turn |
|---|---|---|---|---|
| `entropy/restricted/matrix` | 219ms | 24ms | 29ms | 4ms |
| `expected-size/restricted/matrix` | 264ms | 27ms | 31ms | 5ms |
| `minimax/restricted/matrix` | 485ms | 30ms | 31ms | 5ms |
| `random/restricted/matrix` | 0ms | 1ms | 1ms | 0ms |

Cold open is the first guess of the run, searching the whole space with an empty opening cache. Every warm game reuses that opening, so the two are reported apart -- averaging them would fold a single outlier worth tens of seconds into a per-game figure.

## Candidate collapse

| Solver | After 1 | After 2 | After 3 | After 4 | After 5 |
|---|---|---|---|---|---|
| `entropy/restricted/matrix` | 931.4 | 158.2 | 22.8 | 3.2 | 1.1 |
| `expected-size/restricted/matrix` | 931.4 | 158.2 | 22.8 | 3.2 | 1.1 |
| `minimax/restricted/matrix` | 931.4 | 158.4 | 23.4 | 3.4 | 1.2 |
| `random/restricted/matrix` | 927.1 | 158.6 | 25.3 | 4.3 | 1.4 |

Mean candidates still possible after each guess. The first guess does most of the work; what separates the strategies is how evenly they split what is left.
